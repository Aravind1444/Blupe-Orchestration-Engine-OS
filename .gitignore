.env
.env.*
node_modules/
dist

# Local Netlify folder
.netlify

# OS / editor junk
.DS_Store

# Logs
*.log

# Supabase CLI local state (contains project ref)
supabase/.temp/

# Do NOT ignore cloudrun/**/server.js — those must be deployable from git.
# Root local express server is tracked as server.js (intentionally not ignored).

# IDE / workspace metadata
.claude/
